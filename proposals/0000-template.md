# Proposal: <title>

| | |
|---|---|
| Status | draft / open / accepted / rejected |
| Platforms | macos, android, windows, ios, or core |
| Author | @you |
| Opened | YYYY-MM-DD |

## What a badge claims today

State the current behaviour precisely. Quote the layer from the spec.

## What is wrong with it

An app that does X currently passes, and should not. Or: an app that does Y currently fails, and should not. Be concrete — a repository we can build is worth more than a description.

## Proposed change

The exact wording the spec should have, and which layer status it gets.

## Effect on existing badges

Which currently certified entries would change verdict? Run the proposed check against the registry and say. "None" is an answer, but it has to be a checked one.

## Cost to implement

What has to change in the verifier, and roughly how hard is it in CI.

## Alternatives

Including doing nothing. If the gap is real but the fix is worse than the gap, say so — writing the gap down in `spec/core.md` is a legitimate outcome.
